This directory contains durable artifacts for each agent defined in MULTI_AGENT_TEAM_SPECIFICATION.md.
Each agent has:
- an ADR markdown describing purpose and owner
- a system prompt (`prompt.txt`) used to seed the agent
- an I/O schema YAML (`io_schema.yaml`) describing inputs/outputs and message contract

Artifacts are generated automatically by the developer assistant and should be reviewed before use.
